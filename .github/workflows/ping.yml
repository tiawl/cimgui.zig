name: cd ping

on:
  push:
    tags:
      - '*'

jobs:
  ping:
    permissions:
      contents: write
    strategy:
      matrix:
        repo:
          - spaceporn
    runs-on: ubuntu-latest
    steps:
    - uses: tiawl/spaceporn-dep-action-cd-ping@v1.0.0
      with:
        repository_name: "${{ github.event.repository.name }}"
        user: "${{ matrix.repo }}"
        tag: "${{ github.ref_name }}"
        token: "${{ secrets.PAT }}"
